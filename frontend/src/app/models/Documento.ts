export interface IDocumento {
    definicion: string;
}

export interface IComboBox {
    id: string;
    valor: string;
}

export interface ISimilitud {
    enfoque: string;
    porcentaje: string;
}

export interface IDataset {
    id: string;
    porcentaje: string;
}

export interface IEnfoques {
    nombre: string;
    valor: number;
}